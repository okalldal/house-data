# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Local secrets — Booli identity is per-developer, never commit it
.env

# Scraped artefacts (regenerate with scrape_sold.py); keep the dir, not its contents
cache/
data/*.csv
